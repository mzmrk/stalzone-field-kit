interface:
  display_name: "Maintain Project Memory"
  short_description: "Preserve compact project memory for future agents"
  default_prompt: "Use $maintain-project-documentation before and after this change to load the complete core memory, map structural work to existing architecture, synchronize durable knowledge, and verify it against authoritative source."
